version: '3'
services:
  fe-pss-app:
    build:
      context: ../fe_classification_for_PSS/
      dockerfile: Dockerfile.fe
    container_name: fe-pss
    env_file:
      - .env
    environment:
      TZ: ${TZ}
      WATCHPACK_POLLING: true
    volumes:
      - ../fe_classification_for_PSS/frontend/:/app
    ports:
      - "${FRONTEND_PORT}:${FRONTEND_PORT}" 
    expose:
      - ${FRONTEND_PORT}
    tty: true

  be-pss-app:
    build:
      context: .
      dockerfile: Dockerfile.be
    container_name: be-pss
    env_file:
      - .env
    environment:
      TZ: ${TZ}
    volumes:
      - ./backend/:/app
    ports:
      - "${BACKEND_PORT}:${BACKEND_PORT}"
    expose:
      - ${BACKEND_PORT}
    tty: true
  
  db-pss-app:
    platform: linux/amd64 
    build:
      context: .
      dockerfile: Dockerfile.db
    container_name: db-pss
    env_file:
      - .env
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      TZ: ${TZ}
    command: mysqld --character-set-server=utf8mb4 --collation-server=utf8mb4_unicode_ci
    volumes:
      - ./database/cnf/my.cnf:/etc/mysql/conf.d/my.cnf
      - ./database/log/slow.log:/var/log/slow.log
    ports:
      - "${DATABASE_PORT}:${DATABASE_PORT}"
    expose:
      - ${DATABASE_PORT}
  